import NewProjectComponent from '@/screens/new-project-screen'

export const metadata = {
    title: 'RPA | New Project',
    description: 'Create a new project'
};

const NewProjectScreen = () => {
    return (
        <NewProjectComponent />
    )
}

export default NewProjectScreen